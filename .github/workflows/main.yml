
name: Node CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
#      - name: Checkout
#      - uses: actions/checkout@v2

      - name: Run a one-line script
        run: echo "Hello, world!"
